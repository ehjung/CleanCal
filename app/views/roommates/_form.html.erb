<%= form_for(@roommate) do |f| %>
  <% if @roommate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@roommate.errors.count, "error") %> prohibited this roommate from being saved:</h2>

      <ul>
      <% @roommate.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
